/**
 * Skriva sum JS ikki sum TS, tí reyð strika undir require
 * TS er eitt superset av JS, og kann koyra alla JS kotu
 */

const leftPad = require('left-pad') // Require left pad
const output = leftPad('Hello, World!', 15) // Define output

// Send output to the console
console.log(output)  // small tutorial í console "node index.ts"